# Optional: public URL of the deployed backend API
# Leave empty when using the Vite development proxy.
VITE_API_BASE_URL=

# Optional: Vite development proxy target
# Default: http://127.0.0.1:5000
VITE_DEV_API_TARGET=http://127.0.0.1:5000
